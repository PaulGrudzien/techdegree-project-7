import React from 'react';

function Loading() {
    return (
      <li className="not-found">
        <h3>Loading...</h3>
        <p>Please wait, we are preparing the page</p>
      </li>
    );
};

export default Loading;
